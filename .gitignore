# Godot 4 ----------------------------------------------------------
# インポートキャッシュ(再生成される)。*.import 側は残す。
.godot/
# エクスポート成果物・秘密
export_presets.cfg
/build/
/export/

# 認証情報(絶対にコミットしない) -----------------------------------
.gh_token
.gh_repo

# 作業ファイル ------------------------------------------------------
画像生成/生成ログ.txt
*.tmp
*.log
Thumbs.db
desktop.ini
__pycache__/

# 手動対応中に自動バッチを止めるための一時ファイル(#278再9)
見張り停止.flag
