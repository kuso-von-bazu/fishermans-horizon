# Fisherman's Horizon 引き継ぎ書（旧・2026-07-03時点）

> **この文書は古い。** 当時は issue #23 まで・島5個・3D版からの移行直後の内容。
> 現行の運用（issue枠とラベル、自動バッチ、テスト24スイート、Web版デプロイ手順、
> 画像生成のモデル指定など）は **`引き継ぎ_codex.md` が正**。まずそちらを読むこと。

このプロジェクトを別のモデル/セッション/担当者が引き継ぐための要点。
（Claude のメモリ `MEMORY.md → fishermans-horizon-game.md` にも同等の情報あり）

## 0. 引き継ぎ手順（最短）
1. Claude Code で `/model claude-fable-5`（またはお好みのモデル）に切替。**同じ会話なら文脈もcronも保持される**。
2. 新規セッションで始める場合は、メモリで概要は分かるが **cron（半日巡回）が消える** ので「フィードバック巡回を再開して」と依頼 → 再作成される。
3. この `引き継ぎ.md` を読めば下記すべてが分かる。

## 1. これは何か
拡大再生産型の漁業・ハンティングゲーム（Godot 4.6 / GDScript）。
企画書 `Fisherman's Horizon.docx`（抽出テキスト `plan.txt`）に基づく。
**2026-07-03 にユーザー指示で「見下ろし2D」へ全面再構成し、公開版も2Dに置換。**
- 2D本体 = `scripts2d/`＋`scenes/World2D.tscn`（メインシーン）。
- 3D版コードは残置（`scripts/World.gd` 等＋`scenes/World.tscn`）。戻す場合は project.godot の run/main_scene を World.tscn に。
- GameState/Database/Audio/PortUI/TitleScreen はビュー非依存で両版共用。
- 2Dの要点: 距離換算 K=6.0、北=-Y、敵/魚は生成透過スプライトが本体、円形HPゲージ=Enemy2D._draw、
  海=shaders/ocean2d.gdshader、照準=マウス位置、**Camera2DはWorld直下で追従**（playerの子にすると
  rebuild_visual の子全消しで消えるバグを踏む）。

## 2. 場所・起動・検証
- プロジェクト: `C:\Users\aoe10\漁業ゲーム\`（OneDrive外。.godot同期問題回避のため意図的に）
- Godot本体: `C:\Users\aoe10\Downloads\Godot_v4.6.1-stable_win64.exe\Godot_v4.6.1-stable_win64.exe`
- プレイ実行: `起動.bat` ／ エディタ: `エディタ起動.bat`
- ヘッドレス検証（スクリプトエラー確認）:
  `"<godot>" --path "C:/Users/aoe10/漁業ゲーム" --headless --quit-after 60`
- 画面確認（実ウィンドウで撮影して終了）: 起動引数 `-- "--shot[:sea|:port|:sea:boss|:port:tavern|:sea:food][=保存パス]"`
  例: `... -- "--shot:sea=C:/tmp/x.png"`（World.gd の `_maybe_screenshot`）
- **UI/ダイアログ系は必ず実際に表示させて確認する**（ノードパス等の実行時バグはヘッドレスで出ない）

## 3. GitHub とデプロイ
- 非公開ソース: `kuso-von-bazu/fishermans-horizon`（アカウント kuso-von-bazu）
- 公開プレイ版: `kuso-von-bazu/fishermans-horizon-play`（GitHub Pages）
  → **https://kuso-von-bazu.github.io/fishermans-horizon-play/**
- 認証: `.gh_token`（PAT, gitignore済）を `export GH_TOKEN=$(cat .gh_token)` で使用。gh は `C:\Program Files\GitHub CLI\gh.exe`。リポジトリ名は `.gh_repo`。
- Web書き出し→公開手順:
  1. `"<godot>" --path <proj> --headless --export-release "Web" build/web/index.html`
  2. `cd build/web && git add -A && git commit -m ... && git push origin main`（build/web は play repo の作業ツリー。`.git`と`.nojekyll`は再exportで消えない）
  3. Pages は自動再ビルド（数十秒）。ブラウザは Ctrl+F5。
- Webは `gl_compatibility` レンダラー必須（Forward+はWeb非対応）。`export_presets.cfg` は thread_support=false（どこでも動く）。

## 4. 運用フロー（フィードバック → 改良 → デプロイ）★重要方針
**ユーザー方針: issueが来たら『改良→デプロイまで自動』。よほど致命的でない限り確認せずどんどん反映する。**
1. `bash 意見確認.sh` でオープンな意見Issue取得（半日cron or 手動）
2. 古い順に内容をコード/バランス/UI等へ反映
3. `--headless` でエラー0確認（UIは `--shot` で表示確認）
4. main へ commit/push
5. **Web版に影響する変更なら** build/web を再export → play repo へ push（Pages更新）
6. `gh issue comment <番号>` で反映報告 → `gh issue close <番号>`
7. 仕様判断が要る点はクローズせず質問コメント＋PushNotification
- cron: 半日巡回（9:37/21:37）。セッション内保持・7日で失効 → 失効/再起動時は作り直す。
- 共有者はコラボレーター招待制。招待中: `machichi01-hash`（承認済み）。

## 5. ディレクトリ構成
```
project.godot        設定（autoload: GameState/Database/Audio、入力、gl_compatibility、custom_font）
scenes/World.tscn    薄い（Node3D+World.gd がコードで全構築）
scripts/  GameState(状態) Database(静的データ) World(統合) Player Enemy FishSchool Island
          Projectile Relic HUD PortUI TitleScreen Models(3Dモデル生成) Audio(音)
shaders/  ocean.gdshader(海面波) hpgauge.gdshader(敵HP円形ゲージ)
assets/images  Codex生成PNG（UI挿絵・title.png背景）
assets/audio   numpy合成の効果音9+BGM2 / assets/fonts NotoSansJP.ttf(同梱日本語)
画像生成/  Codex image_gen ランナー＋透過処理.py（.gdignoreでGodot除外）
音声生成.py  効果音・BGM合成
build/web    Web書き出し先（play repo の作業ツリー、.gitignore）
```

## 6. アセット生成
- 画像（API不要・Codex image_gen）: `画像生成/画像生成_codex.sh`（codex.exe自動検出・`</dev/null`必須・生成後 透過処理.py）。出力 `assets/images/<種別>_<id>.png`。**落とし穴: 出力はアルファ無しの近白背景→透過処理.py 必須**。
- 島の絵（#278 提案2）: `画像生成/島生成_codex.sh`（上空視点・**背景はマゼンタ**）→ `島透過.py` → `ドット絵化.py`(256px/64色)。
  近白背景を抜く `透過処理.py` だと雪原の島（果て・北の孤島）が消えるためマゼンタキーにしている。
- 音声（numpy）: `python 音声生成.py` → `assets/audio/*.wav`。**既存wavも上書きされる**ので、
  1音だけ足したいときは生成後に不要な差分を `git checkout --` で戻すこと。
- タイトルキーアート: 画像生成を透過処理なしで（全面背景用）。

## 7. 実装上の落とし穴（重要）
- Godotは `add_child` で `_ready` が走る → `node.setup(...)` は **add_child の前**に呼ぶ（後だと未設定でクラッシュ）。`global_position` は tree 内必須なので add_child 後。
- 独自クラスは **`class_name` を使わず `const X = preload(...)`**（インポート前の直起動で未登録になるため）。
- HUDの手動配置パネルは **`anchor_* + offset_*` を明示指定**（`set_anchor + position + size` の組合せだと非表示になった実績）。
- **Web文字化け**: 既定フォントはCJK非対応。`assets/fonts/NotoSansJP.ttf`（OFL）を同梱し `[gui] theme/custom_font` に設定済み。新規フォントは `--headless --import` してから使う。
- 新規PNGは実行前に `--headless --import` しないと `ResourceLoader.exists` が false。
- git は CRLF 警告が出るが無害。commit は `git -c core.autocrlf=false`。

## 8. 現状（2026-07-03 時点）
- コア（漁・戦闘・港経済・複数島・ボス固有挙動・タイトル/勝利）実装済み・Web公開済み。
- 共有フィードバック **#1〜#23 まで対応・クローズ済み（残オープン0）**。
- 直近: 敵HP円形ゲージ(#21)、航路の方角表示(#22)、食料半減選択ダイアログのバグ修正(#23)。

## 9. 次にやると良いこと（任意・未依頼）
- クルー要素／船団要素（企画書で「当初は組み込まなくていい」＝保留中）。
- さらなる見た目強化（空/光/航跡、船の被弾演出）、モバイル操作対応、バランス調整。
- BGMの質向上（現状はnumpy簡易合成）。
