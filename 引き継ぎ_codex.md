# Fisherman's Horizon — Codex 引き継ぎ書

作成: 2026-09-11 / 前任: Claude (Opus 5 / Fable 5)

これまで Claude が担っていた「GitHub issue を受けて改良し、検証してデプロイし、報告してクローズする」
という運用を **Codex が引き継ぐ**ための書類。これ1本読めば作業を始められるように書いてある。

> 旧 `引き継ぎ.md` は 2026-07-03 時点の内容で**大きく古い**（当時は issue #23 まで、島5個）。
> 迷ったらこちらを正とすること。

---

## 0. 最初にやること

```bash
cd /c/Users/aoe10/漁業ゲーム
export GH_TOKEN=$(cat .gh_token)      # 必ず絶対パスか、このフォルダで実行すること
bash 意見確認.sh                       # 未対応の意見issueが出る
```

いま **#293「パッド操作の実装について」が未対応で残っている**。これが最初の仕事。
（ゲームパッド対応。航海中と島メニューの2系統、割り当ては issue 本文に全部書いてある）

---

## 1. これは何か

拡大再生産型の漁業・ハンティングゲーム。**Godot 4.6.1 / GDScript / 見下ろし2D**。

- プレイヤーは漁で稼ぎ、船を買い、船団を組み、近海の主を討伐して名声を稼ぎ、次の島へ進む
- 公開先: https://kuso-von-bazu.github.io/fishermans-horizon-play/
- 共有者（レビュアー）の `machichi01-hash` が GitHub issue で要望・不具合を送ってくる。それに応えるのが主業務

### 規模（2026-09-11 時点）

| 要素 | 数 |
|---|---|
| 島 | 11 |
| 近海の主（ボス） | 20（分裂体含む） |
| 戦闘能力があるモブ | 20 |
| 魚 | 11 |
| 船 | 10 |
| 武器 | 13（＋衝角3） |
| 実績 | 51 |
| 自動テスト | 24スイート |
| issue | #293 まで（ほぼ全件クローズ済み） |

### 構成

```
project.godot     autoload: Database / GameState / Audio / PortUI / TitleScreen
                  レンダラは gl_compatibility（Web必須）。custom_font に NotoSansJP
scenes/           World2D.tscn がメインシーン（中身は薄く、コードで全構築）
scripts/          ビュー非依存の中核。Database(静的データ) GameState(状態) PortUI(港UI)
                  Audio TitleScreen PixelFont OverlayMenus AchievementToast
                  ※ World.gd / Player.gd / Enemy.gd 等は旧3D版の残骸。**触らなくてよい**
scripts2d/        2D本体。World2D(統合) Player2D Enemy2D Escort2D(僚艦) FishSchool2D
                  Projectile2D HUD2D Island2D Obstacle2D Relic2D Flotsam2D Juice2D LosOverlay2D
shaders/          ocean2d(海) weather2d(雨雪夜) hpgauge
assets/images/    一枚絵（生成元）。assets/images/pixel/ がゲームで使うドット絵
assets/audio/     効果音・BGM   assets/fonts/ NotoSansJP + PixelMplus12
tests/            maintenance_issue_*.gd + .tscn（後述）
tools/            敵性能一覧 / 実績一覧 / 武器一覧 / 射線計測（データ書き出し用）
画像生成/          Codex image_gen で絵を作るスクリプト群
build/web/        Web書き出し先。**これ自体が別リポジトリの作業ツリー**（後述）
```

**データはほぼ `scripts/Database.gd` に集中している。** 敵・主・船・武器・島・実績・出港ヒントが
全部ここの辞書。バランス調整依頼の8割はこのファイルの数値を触るだけで終わる。

---

## 2. 実行・検証

Godot本体（**console 版を使うこと**。出力が取れる）:

```
/c/Users/aoe10/Downloads/Godot_v4.6.1-stable_win64.exe/Godot_v4.6.1-stable_win64_console.exe
```

### パースエラーの確認（最速）

```bash
GODOT="/c/Users/aoe10/Downloads/Godot_v4.6.1-stable_win64.exe/Godot_v4.6.1-stable_win64_console.exe"
"$GODOT" --headless --path . --quit-after 200 2>&1 | grep -E "SCRIPT ERROR|Parse Error"
```

### 自動テスト（**必ず全部通してからpush**）

```bash
"$GODOT" --headless --path . tests/maintenance_issue_290.tscn
# → MAINTENANCE_TEST_OK ... と出れば成功
```

24スイートあり、**全部まとめて回すと10分を超えて Bash がタイムアウトする**。
6〜7本ずつに分けて回すこと。1本あたり最長200〜280秒を見ておく。

テストは「issue番号ごとのファイル」という作りで、過去の要望が壊れていないかを守っている。
**数値を変えたら、その数値を固定しているテストが落ちる。** 落ちたら中身を読んで、
「仕様変更なら期待値を更新」「意図しない破壊なら実装を直す」を判断する。

### 画面の確認（スクリーンショット）

ヘッドレスでは描画されない。**実ウィンドウで起動して撮る**。

```bash
"$GODOT" --path . -- --shot:sea:isle10          # 雪夜の島の近海
"$GODOT" --path . -- --shot:port:tavern:lords   # 酒場の主の情報
"$GODOT" --path . -- --shot:port:yard:isle8     # 北の孤島の造船所
"$GODOT" --path . -- --shot:port:route          # 航路メニュー
"$GODOT" --path . -- --shot:port:ach4           # 実績メニュー（主の枠）
```

保存先は固定で **1枚だけ**上書きされる:
`C:/Users/aoe10/AppData/Roaming/Godot/app_userdata/Fisherman's Horizon/shot.png`
→ 撮ったらすぐコピーする。複数撮るなら1枚ずつ。

- 指定の一覧と実装は `scripts2d/World2D.gd` の `_maybe_screenshot()`
- `--quit-after` を付けると**撮影前に終了してしまうことがある**ので付けない（内部で自動終了する）
- `:isle10` のように島は2桁まで指定できる

---

## 3. GitHub と デプロイ

- 非公開ソース: `kuso-von-bazu/fishermans-horizon`（このフォルダ）
- 公開プレイ版: `kuso-von-bazu/fishermans-horizon-play` = **`build/web/` がその作業ツリー**
- 認証: `.gh_token`（PAT）。**gitignore 済み。絶対にコミットしない**

### Web版を更新する手順（**ゲームに影響する変更なら必須**）

```bash
GODOT="/c/Users/aoe10/Downloads/Godot_v4.6.1-stable_win64.exe/Godot_v4.6.1-stable_win64_console.exe"
"$GODOT" --headless --path . --export-release "Web" build/web/index.html
ls build/web/*.tmp | wc -l        # 0 なら成功。残っていたら書き出し失敗
cd build/web && git add -A && git commit -m "..." && git push origin main
```

> **過去に何度も「ソースだけ直してデプロイし忘れる」事故が起きている。**
> 公開版が古いままだとレビュアーには直っていないように見える。
> issue を閉じる前に `build/web` の最新コミットがソース側と対応しているか確認すること。
> `index.pck` が80MB超で GitHub が警告を出すが、無視してよい（LFS化は未実施）。

---

## 4. 運用フロー（これが本業）

**ユーザー方針: issueが来たら『改良→デプロイまで自動で進める』。よほど致命的でない限り、確認せずどんどん反映してよい。**

1. `bash 意見確認.sh` で未対応issueを取得
2. **本文だけでなくコメント欄を全部読む**（後述の落とし穴）
3. 実装する
4. テストを通す（必要なら新しい検査を足す）
5. `git commit && git push`
6. **Web版に影響するなら再export → build/web を push**
7. `gh issue comment <番号> -b "✅反映: ..."` → `gh issue close <番号>`
8. 仕様判断が要る点はクローズせず、質問コメントを残す

### ラベルと枠

| ラベル | 意味 | 自動処理 |
|---|---|---|
| `feedback` | 通常の意見・不具合 | **する** |
| `画像生成` | 絵の新規生成を伴う | **する** |
| `コンサル` | 答えが決まっていない相談 | **絶対にしない** |
| `製作依頼` | エディタ等の道具の製作依頼 | **絶対にしない** |

`コンサル` と `製作依頼` は**開発者が別のタイミングで手動対応する枠**。
`意見確認.sh` は最初からこの2つを除外して表示する（件数だけ知らせる）。触らないこと。

### 30分おきの自動バッチ「issue見張り」

`C:\Users\aoe10\issue見張り\` にタスクスケジューラ登録済み。30分ごとに issue を確認し、
新規/更新があるときだけ `claude -p` を起動していた。**Codex へ切り替えるなら
`設定.json` の起動コマンドを差し替えること**（`見張り.py` の `run_target()` 内）。

バッチには次のガードが入っている（自分と手作業がぶつかるのを防ぐため）:

- 追跡中のファイルに**未コミットの変更があるフォルダは起動を見送る**
- フォルダに `見張り停止.flag` を置くと、変更が無くても見送る

→ **手で作業するときは `見張り停止.flag` を置き、終わったら消す。**
→ 作業を中断したまま未コミットで放置すると、バッチが永久に起動しなくなる（実際に12時間止めた）。

---

## 5. 絵の作り方（Codexの本来の得意分野）

```bash
bash 画像生成/画像生成_codex.sh                    # プロンプト.csv 全件（既存はスキップ）
bash 画像生成/画像生成_codex.sh lord_gemini.png    # 指定ファイルだけ
bash 画像生成/画像生成_codex.sh -f                  # 既存も上書き
```

1. `画像生成/プロンプト.csv` に `ファイル名,プロンプト` を追記
2. 上のスクリプトで `assets/images/` に一枚絵を生成（生成直後に `透過処理.py` が走る）
3. `python 画像生成/ドット絵化.py lord_xxx.png ...` で `assets/images/pixel/` にドット絵化
4. `"$GODOT" --headless --path . --import`（**これをしないと `ResourceLoader.exists` が false**）

### 重要な注意

- **モデルは `-m gpt-5.6-sol` を明示指定している。** 既定のままだと
  `The 'gpt-6-astra' model requires a newer version of Codex` で全滅する（2026-09-05に踏んだ）
- `codex exec` には **`</dev/null` が必須**。付けないと CSV を食って2件目で止まる
- 生成物は**アルファ無しの近白背景**。透過処理が必要
- **島の絵だけは別**: `画像生成/島生成_codex.sh`（背景マゼンタ）→ `島透過.py` → `ドット絵化.py`。
  雪原の島が近白背景の透過処理で消えてしまうため、マゼンタキーにしてある
- プロンプトには必ずこの一文を入れる（額縁の線や断片が焼き付くのを防ぐ）:
  > 背景は完全な透明(市松模様・白い下地・枠線・額縁・画像の縁の直線を一切残さない)。被写体の周囲に霧・靄・光のグロー・背景の板を描かない。体から離れた断片を作らず、すべての部位が体につながった一つのシルエットにする。被写体は画面いっぱいに大きく描き、全身が画面内に完全に収まるようにする。
- `ドット絵化.py` は**引数なしだと全PNGを作り直す**。必ずファイル名を指定すること
- 音声: `python 音声生成.py` は**既存wavも全部上書きする**。1音だけ足したいときは、
  生成後に不要な差分を `git checkout --` で戻す

---

## 6. 落とし穴（全部実際に踏んだもの）

### 作業環境

- **ファイルは CRLF。** Python で編集するときは必ず
  `io.open(p, encoding='utf-8', newline='')` で読み、改行は `'\r\n' if '\r\n' in s else '\n'` を使う。
  これを忘れるとファイル全体が差分になる
- **Windows のコンソールが日本語を化けさせる。** 結果を確認したいときは
  ファイルに UTF-8 で書き出してから読むこと
- **`subprocess` で `text=True` を使わない。** cp932 で復号され、日本語ファイル名が
  含まれるだけで `stdout` が `None` になる。バイトで受けて `.decode('utf-8')` する
  （これで見張りバッチが12時間止まった）
- 長いヒアドキュメントは bash が壊すことがある。スクリプトはファイルに書いて実行する

### Godot / GDScript

- `add_child` で `_ready` が走る → `setup()` は **add_child の前**に呼ぶ
- 独自クラスは `class_name` を使わず **`const X = preload(...)`**
- 新規アセットは **`--headless --import` するまで `ResourceLoader.exists` が false**
- **`TextureRect.expand_mode` の既定は `EXPAND_KEEP_SIZE`** で、最小サイズが元画像サイズになり
  `custom_minimum_size` を無視する。枠に収めたいなら `EXPAND_IGNORE_SIZE` ＋ `SIZE_SHRINK_CENTER`
- **`StyleBoxFlat` の `shadow` は輪郭線ではなく「パネル全面の塗り」**。
  これに気づかず背景の不透明度を4回下げて「変わらない」と言われ続けた（#278）
- Camera2D は World 直下に置く（player の子にすると `rebuild_visual` の子全消しで消える）

### このゲーム固有

- **敵HPの丸め**: `Database.scaled_hp()` は「4桁は100の倍数へ切り上げ / 5桁は1000の倍数へ切り捨て」。
  そのため 10500 や 2556 のような値は基礎HPからは作れない。
  → 出現島が固定の主は **`hp_exact`**（その島でだけ使う実値）で直接指定する。
  他の海域（ボスラッシュ＝果ての島扱い）では従来どおり基礎HP×倍率
- **`side_only`**: 横向き1枚だけ使う敵（幽霊船・キラーシェル等）。前後の絵は**ファイルごと消す**。
  残すと `ドット絵化.py` の一括実行で復活する
- **`face_left`**: 絵が左を向いているときだけ付ける。絵を作り直すと向きが変わるので毎回確認する
- **島を増やすと壊れる場所**: 海シェーダの配列長（`islands[11]` 等）、
  障害物の数の表、撮影引数の島番号。島の数に連動する配列を全部探すこと
- **Database の辞書は1行が長い。** `"color":` などは入れ子の `burst` / `death_shot` にも出てくるので、
  素朴な文字列置換で入れ子の中に値を挿入してしまう事故が起きた。
  挿入するなら**波括弧の深さを数えて**depth-1 の位置を狙う

### issue 対応の落とし穴

- **本文だけ読んで閉じると追加要望を取りこぼす。** 必ずコメント欄を最後まで読む（過去に5件やらかした）
- **コメントは編集される。** 長い作業の途中で仕様が増えていることがあるので、着手直前に読み直す
- **テストに判定式を書き写さない。** 過去のテストが「実装と同じ式をテスト側にも書いて比較」していて、
  実装がどうなっていても通る状態だった。**実装の値・表そのものを読む**こと
- 修正したら**必ず「戻すと落ちる」ことを確認**する（ネガティブ確認）。
  これをやったおかげで、通っているつもりの空振りテストを何度も見つけている

---

## 7. いま残っている論点（未決）

レビュアーの判断待ちで、こちらからは動かしていないもの。聞かれたら答えられるように。

1. **実績バフと陣形の逆転**
   全実績のバフを2倍にした結果（#265）、実績の最大が **+6%** になり、
   陣形の最小である単縦陣 **前進最高速+5%** を上回った。
   当初 #265 では「バフはおまけ程度で陣形を大きく下回るように」という指定だったので、設計とはズレている。
   維持したいなら上限4%程度（2倍ではなく1.3倍）が元の意図に沿う

2. **稼ぎ半減と船価格の据え置き**
   島の販売倍率を全体的に下げた（#96、tier4 は 10.8 → 5.4）が、**船の価格は据え置き**。
   終盤に船を揃えるのに必要な狩りの回数が実質2倍になっている。
   例: ザッハーク 16,848 → 8,424 に対し、巨大戦艦は 120,000 のまま

3. **雪夜の島の位置づけ**
   #290 で追加した雪夜の島は tier4 なので、**到達した瞬間に最高倍率**（5.4）になる。
   果ての島（名声480）より手前（名声400）にあるため、最高倍率に届くのが1島ぶん早くなった

---

## 8. 参考資料

- `運用_意見ワークフロー.md` — issue の枠・ラベル・巡回の運用（バッチのガードも記載）
- `コンサルレポート_2026-08-14.md` — ①UI過密 ②航海の待ち時間 ③武器の固定化 ④陣形スキルの薄さ
- `コンサルレポート②_2026-08-20.md` — 僚艦の射線ブロック問題（実測付き。計測器 `tools/射線計測.tscn`）
- `コンサルレポート③_2026-08-28.md` — 見た目の統一（海のドット絵化ほか）。**大半が実施済み**
- `tools/敵性能一覧.tscn` — 全敵の島別HP・攻撃力・間隔・速度を Markdown に書き出す。
  バランス依頼の検算はこれで行う（ヘッドレスで実行、出力先はコンソールに表示される）

---

## 9. 引き継ぎにあたっての一言

このプロジェクトの価値は**レビュアーとの往復の速さと正確さ**にある。
数値の指定は具体的に飛んでくるので、**言われた値に本当になっているかを実データで検算してから報告する**こと。
「反映しました」と書いたのに実際は丸められていた・デプロイし忘れていた、という事故が
これまでの失敗のほとんどを占めている。

テストは資産。増やすほど安全に速く回せるようになる。
逆に、期待値を惰性で書き換えるだけだと**守っているつもりの空洞**になる。
落ちたときは必ず「仕様が変わったのか、壊したのか」を判断してから直すこと。
