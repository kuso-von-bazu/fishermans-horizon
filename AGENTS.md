# Fisherman's Horizon の作業ルール

## 作業開始時に読む資料

1. `引き継ぎ_codex.md` を全文読む。現行の引継書はこちら。
2. `運用_意見ワークフロー.md` を読み、対象ラベルと自動バッチの競合防止を確認する。
3. 依頼に関係する実装・テストを読む。issue対応では最新の本文と全コメントを取得し、長時間の作業では反映前にも再確認する。

`引き継ぎ.md` は2026-07-03時点の旧資料。READMEにも旧3D版の説明があるため、現行仕様の根拠にしない。引継書の件数・未対応issue・構成も固定の事実とせず、現在のGitHubと実ファイルで確認する。ユーザーの今回の依頼を優先し、引継書に記載された次の仕事を、準備や確認だけの依頼中に自動で着手しない。

## 開始・終了時

- 作業場所はこのリポジトリ。ソースと `build/web` は別リポジトリなので、それぞれの変更状況を確認し、既存の変更を巻き込まない。
- 編集前に `見張り停止.flag` を置く。既存のフラグは他の作業者のものとして保持する。自分で作成したフラグだけを作業完了時に除去する。
- 未完了作業・検証結果・残課題・公開状況を報告する。引継内容に変更があれば `引き継ぎ_codex.md` を更新する。
- 自動巡回の設定変更は、別途その作業を依頼されたときに行う。

## 実装と検証

- Godot 4.6.1 / GDScript の見下ろし2D。メインは `scenes/World2D.tscn`、本体は `scripts2d/`、共通処理とデータは `scripts/`。旧3Dコードは対象の依頼がない限り触らない。
- `project.godot` の現行設定を確認する。Web向けの `gl_compatibility` を維持する。
- 既存ファイルの改行を維持する。数値変更は丸め処理を含む実際の値で検証する。
- Godotのconsole版を使い、引継書の手順で検証する。ゲーム変更をpushする前に全テストスイートを通す。UI変更は実画面でも確認する。
- ゲーム変更を公開する際はソースだけで終えず、Webを書き出して `build/web` の公開版も更新・確認する。未検証・未公開を完了扱いにしない。

## issueと認証情報

- 自動処理で `コンサル` と `製作依頼` ラベルのissueを扱わない。
- `.gh_token` は認証にのみ使用し、画面・ログ・差分へ出力せず、コミットしない。
- issueコメント投稿など他者への送信は、会話でその操作が明示的に許可されている範囲で行う。

## 引継確認時の記録（2026-09-11）

- ローカル: `C:\Users\aoe10\漁業ゲーム`
- ソース: `kuso-von-bazu/fishermans-horizon`
- 確認時のHEAD: `7c462d2`（Codexへの引継書を作成）。開始時に未コミット変更なし。
- GitHubのopen issueは #293「パッド操作の実装について」1件。本文確認済み、コメント0件。実装着手時に再取得する。
- PowerShellからGit Bashのスクリプトを直接起動すると `dirname` / `tr` がPATHに無い場合がある。Git Bashの環境を整えるか、PowerShellとGitHub CLIで同じ読み取りを行う。

## #293 対応追記（2026-09-11）

パッド対応を実装・検証。現行の操作・構成・25スイートの検証手順は `引き継ぎ_codex.md` の #293 追記を参照する。上記のopen issue記録は着手前の履歴であり、現在の状態はGitHubで再確認する。
