# 共有者の意見を拾って改良するワークフロー

Fisherman's Horizon を共有した人が GitHub の **Issue** に意見を書き、開発側（このPCの Claude が `/loop`）が
それを拾って改良 → 反映 → コメント → クローズする運用。

## 共有者側（意見を書く人）
テンプレートは4つある。**絵を新しく生成する必要があるか**、**答えが決まっているか**、**ゲーム本体かそれ以外の道具か**で選び分ける。

| 選ぶテンプレート | 使う場面 | タイトル | 付くラベル |
|---|---|---|---|
| ゲームへの意見・フィードバック | 挙動・バランス・UI配置など。既存の絵で足りる | `[意見] ` | `feedback` |
| 画像生成をともなう要望 | 新しい敵・アイコンなど絵を描き起こす必要がある | `[画像] ` | `feedback` + `画像生成` |
| コンサル希望 | 答えが決まっていない相談。仕組みの是非・案の比較・方向性 | `[コンサル] ` | `コンサル` |
| 製作を依頼(各種エディタなど) | ゲーム本体ではなく作業を助ける道具。データ編集エディタ・検証ツール・一覧出力 | `[製作] ` | `製作依頼` |

分けている理由は、**Codex の画像生成コストを見積もるため**。issue 一覧を見たときに、
タイトルの接頭辞（`[画像]`）とラベルの色で、絵の案件かどうかが一目で分かるようにしている。
画像生成をともなう要望では「絵の種類」と「必要な枚数の概算」も聞いている。

絵の案件だけを見るには:
```bash
gh issue list --label 画像生成 --state all
```

## 開発側（このPC）の準備（最初の一度だけ）
1. このフォルダに `.gh_token` を作成し、GitHub の Personal Access Token（classic, `repo` スコープ）を1行で保存
2. このフォルダに `.gh_repo` を作成し、`owner/リポジトリ名`（例 `kuso-von-bazu/fishermans-horizon`）を1行で保存
   - どちらも `.gitignore` 済みでコミットされない

## 処理ループ（Claude が `/loop` で回す）

> **重要: `コンサル` と `製作依頼` ラベルの issue は自動処理の対象外。**
> どちらも開発者が Fable に切り替えて手動で扱うため、自動巡回では
> **拾わない・回答しない・クローズしない**。オープンのまま残す。
> 巡回時は次のように除外して取得すること。
>
> ```bash
> gh issue list --state open --search "-label:コンサル -label:製作依頼"
> ```

1. `bash 意見確認.sh` … オープン中の意見を古い順に取得（`コンサル`・`製作依頼` ラベルを除く）
2. 内容を反映（スクリプト/シーン/モデル/バランス値の編集）
3. **検証**: `--headless --quit-after` でエラー0を確認し、必要なら `--shot` でスクショ確認
4. `git add/commit/push`
5. `gh issue comment <番号> -b "✅反映: 〜（検証結果）"` → `gh issue close <番号>`
6. 仕様判断が要る点はクローズせず質問コメントを残す

## 自動バッチと手作業がぶつからないようにする

issue対応は30分おきのタスクスケジューラ「issue見張り」が自動で回している
(`C:\Users\aoe10\issue見張り`)。ここで開発者が同じフォルダを手で触っていると、
**編集途中のファイルをバッチが自分の変更として巻き込んでコミットしてしまう**
(2026-09-02に実際に発生した)。

そのため見張り側に次のガードを入れてある。

- 追跡中のファイルに**未コミットの変更があると、そのフォルダは起動を見送る**
  (作業中とみなす。ログに「未コミットの変更がN件ある」と出る)
- フォルダに `見張り停止.flag` を置くと、変更が無くても見送る(長めに止めたいとき用)

つまり、手で作業するときは**コミットせずに作業を続けていればバッチは入ってこない**。
逆に、作業を中断してコミット済み・作業ツリーが空の状態で放置すると、
次の巡回でバッチが続きを拾う可能性がある。

## ラベル運用
- `feedback`（意見・フィードバック。フォームで自動付与）
- `コンサル`（コンサル希望。フォームで自動付与。**自動処理では触らない**）
- `製作依頼`（エディタ等の製作依頼。フォームで自動付与。**自動処理では触らない**）
- `画像生成`（絵の新規生成が必要＝Codexの生成コストが発生。フォームで自動付与）
- `urgent`（急ぎ。手動）
- `wontfix` / `question`（対応しない／要相談）

## メモ
- バイナリの生成画像（assets/images/*.png）はリポジトリに含める（数MB）。
- 画像生成には Codex の利用枠を消費する。枠を使い切ると `codex exec` が
  `ERROR: You've hit your usage limit` を返して**生成が丸ごと失敗する**。
  スクリプトは失敗としか出さないので、原因切り分けは `codex.exe exec` を直接叩いて
  生の出力を見ること（スクリプトは出力を `/dev/null` に捨てている）。
- `.godot/` はインポートキャッシュなので含めない（クローン後に Godot を開けば再生成）。
